{
  "name": "mandelbrot-renderer",
  "version": "2.0.0",
  "description": "High-performance fractal rendering engine with multiple parallel computing implementations",
  "main": "docs/mandelbrot.js",
  "scripts": {
    "dev": "npx http-server ./docs -p 8080 -o",
    "dev:api": "cd server && node index.js",
    "test": "echo \"Test framework to be implemented\" && exit 0",
    "lint": "eslint docs/*.js --ignore-pattern '*.min.js'",
    "format": "prettier --write \"docs/**/*.{js,html,css}\"",
    "format:check": "prettier --check \"docs/**/*.{js,html,css}\"",
    "build:cpp": "mkdir -p build && cd build && cmake .. -DCMAKE_BUILD_TYPE=Release && make -j$(nproc)",
    "build:api": "mkdir -p build && cd build && cmake .. && make fractal_api",
    "build:wasm": "cd wasm && bash build.sh",
    "docker:up": "docker compose up -d --build",
    "docker:down": "docker compose down",
    "docker:logs": "docker compose logs -f",
    "deploy": "echo \"Deployed via GitHub Actions or docker compose up\"",
    "benchmark": "node scripts/benchmark.js"
  },
  "repository": {
    "type": "git",
    "url": "git+https://github.com/GeoffreyWang1117/Mandelbrot-Renderer.git"
  },
  "keywords": [
    "fractal",
    "mandelbrot",
    "julia-set",
    "burning-ship",
    "newton-fractal",
    "webassembly",
    "cuda",
    "openmp",
    "parallel-computing",
    "gpu-computing",
    "visualization",
    "mathematics",
    "computational-art",
    "performance"
  ],
  "author": "Geoffrey Wang",
  "license": "MIT",
  "bugs": {
    "url": "https://github.com/GeoffreyWang1117/Mandelbrot-Renderer/issues"
  },
  "homepage": "https://geoffreywang1117.github.io/Mandelbrot-Renderer/",
  "devDependencies": {
    "eslint": "^8.57.0",
    "prettier": "^3.2.5",
    "http-server": "^14.1.1"
  },
  "engines": {
    "node": ">=14.0.0",
    "npm": ">=6.0.0"
  }
}
